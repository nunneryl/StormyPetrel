requests>=2.31,<3
rapidfuzz>=3.6,<4
tenacity>=8.2,<9
tqdm>=4.66,<5
reverse_geocoder>=1.5.1,<2

# Phase 0C — LLM tide classification
anthropic>=0.40,<1

# Phase 2C — direct scrape of surf-forecast.com
beautifulsoup4>=4.12,<5

# Phase 1 — NWPS GRIB2 wave forecasts
# cfgrib requires the eccodes system library:
#   Debian/Ubuntu: apt install libeccodes0
#   macOS:         brew install eccodes
xarray>=2024.1
cfgrib>=0.9.14

# Enrichment (Phase 0B) — geospatial stack
shapely>=2.0,<3
pyproj>=3.6,<4
fiona>=1.9,<2
geopandas>=0.14,<2
rtree>=1.1,<2
scipy>=1.11,<2
numpy>=1.26,<3
